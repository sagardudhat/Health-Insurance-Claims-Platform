PORT=5000
MONGO_URI=mongodb://localhost:27017/claimcare
JWT_SECRET=super_secret_jwt_key_claimcare_2026
NODE_ENV=development

# Optional SMTP Configuration (If not provided, system uses Ethereal test SMTP / Console logger)
SMTP_HOST=smtp.example.com
SMTP_PORT=587
SMTP_USER=user@example.com
SMTP_PASS=password
EMAIL_FROM="ClaimCare Health" <no-reply@claimcare.health>
